<div class="row justify-content-center">
  <div class="col-8">
    <!-- <div class="row justify-content-center">
      <div class="btn-group mb-4">
        <a href="/schedules?show=all" class="btn btn-outline-secondary">Show All</a>
        <a href="/schedules?show=free" class="btn btn-outline-secondary">Show Free Only</a>
      </div>
    </div> -->
    <% for (let schedule of schedules) { %>
    <div class="card mb-4" <%= colorizeSchedule(schedule.free) %>>
      <div class="card-body">
        <h5 class="card-title">
          <a href=<%= `/schedules/${schedule.id}` %>><%= schedule.title %></a>
        </h5>
        <h6 class="card-subtitle mb-2 text-muted"><%= formatTime(schedule.date) %></h6>
        <p class="card-text"><strong>Venue:</strong> <%= schedule.Restaurant.name %>,
          <%= schedule.Restaurant.location %></p>
      </div>
    </div>
    <% } %>
  </div>
</div>